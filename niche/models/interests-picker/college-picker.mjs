export class CollegePicker {
    constructor(page) {
        this.page = page;
        // locators
        this.transfer = '#consideringTransfer';
        this.k12 = '#consideringGrad';
        this.live = '#consideringNeither';
    }
}